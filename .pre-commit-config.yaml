repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  # mypy requires the full project virtualenv; run via CI rather than pre-commit.
  # To run locally: mypy src/agentic_rag
